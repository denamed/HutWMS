<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>BUILDINGS INFO</title>
    <link rel="stylesheet" type="text/css" href="style.css" />
</head>
<body>
<table class="body">
    <tr>
        <td class="left">

        </td>
        <td class="right">

            <table class="ctable">
                <tr><td class="caption">BUILDINGS</td></tr>
                <tr><td class="alert">{{#message}}{{{message}}}{{/message}}</td></tr>
                <tr><td>
                    <div class="nav">
                    <form method="get"><button type="submit" formaction="/">< Back</button></form>
                    <form method="get"><button type="submit" formaction="/building">Refresh</button></form>
                    <form method="get"><button type="submit" formaction="/building-create">Add +</button></form>
                    </div>
                </td></tr>

                <tr><td>ID Description

                {{#buildings}}
                    <tr>
                        <td>{{buildId}}
                        {{buildName}}
                        <form method="get">
                            <input type="number" value="{{buildId}}" name="buildId" hidden/>
                            <button type="submit" formaction="/building-edit">Edit</button>
                        </form>
                        <form method="post">
                            <input type="number" value="{{buildId}}" name="buildId" hidden/>
                            <button type="submit" formaction="/building-delete" onclick="return confirm('Are you sure?');">Delete</button>
                        </form></td>
                    </tr>
                {{/buildings}}
                {{^buildings}}
                    <tr><td>No buildings added yet.</td></tr>
                {{/buildings}}
            </table>

        </td>
    </tr>
</table>
</body>
</html>
