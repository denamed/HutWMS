<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Building edit</title>
    <link rel="stylesheet" type="text/css" href="style.css" />
</head>
<body>
<table width="100%" class="body"><tr><td align="center">

    <table class="table">
        <caption>BUILDING EDIT</caption>
        <thead>
        <tr>
            <th width="72"><form method="get"><button type="submit" formaction="/building">< Back</button></form></th>
            <th colspan="2">{{#message}}{{{message}}}{{/message}}</th>
            <th width="72"><form method="get">
                <input type="number" name="buildId" value="{{#building}}{{buildId}}{{/building}}" hidden/>
                <input type="text" name="buildName" value="{{#building}}{{buildName}}{{/building}}" hidden/>
                <button type="submit" formaction="/building-edit">Reset</button>
            </form></th>
        </tr>
        </thead>
        <form method="post">
            <tr>
                <td colspan="2"><label>Building ID:</label></td>
                <td><input type="number" name="buildId" value="{{#building}}{{buildId}}{{/building}}" hidden required/>
                    <input type="number" value="{{#building}}{{buildId}}{{/building}}" disabled required/></td>
                <td></td>
            </tr><tr>
            <td colspan="2"><label>Building Name:</label></td>
            <td><input type="text" maxlength="16" name="buildName" value="{{#building}}{{buildName}}{{/building}}" required/></td>
            <td></td>
        </tr><tr>
            <td colspan="4"><button type="submit" formaction="/building-edit">Save</button></td>
        </tr>
        </form>
    </table>

</td></tr></table>
</body>
</html>